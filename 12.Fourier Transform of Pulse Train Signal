fy=1;
wy=2*pi*fy;
Ty=1/fy;
N=256; W=20; 
fs=N*fy;
y1=zeros(256,1);
y1(1:W)=1; y1((256-W):256)=1;
fou=fft(y1,fs); hmag=real(fou); 
ah=hmag/N;
stem(0:49,ah(1:50)); hold on;
plot([0 50],[0 0],'k');
xlabel('Hz'); 
title('pulse train signal harmonics'); 
