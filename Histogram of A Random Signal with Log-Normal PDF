fs=100; 
tiv=1/fs; 
t=0:tiv:(100-tiv); 
N=length(t); 
mu=0; 
sigma=1;
y=lognrnd(mu,sigma,N,1); 
v=0:0.1:12; 
hist(y,v); 
colormap(cool); 
axis([0 8 0 700]);
xlabel ('values');
title('Histogram of random signal with log-normal PDF');
